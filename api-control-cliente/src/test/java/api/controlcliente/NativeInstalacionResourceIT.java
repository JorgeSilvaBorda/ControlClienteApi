package api.controlcliente;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeInstalacionResourceIT extends InstalacionResourceTest {

    // Execute the same tests but in native mode.
}